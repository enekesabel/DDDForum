export class ContactListAPI {
  // will not implement communicating with a real api for the time being
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  async addEmailToList(_email: string): Promise<void> {
    return;
  }
}
